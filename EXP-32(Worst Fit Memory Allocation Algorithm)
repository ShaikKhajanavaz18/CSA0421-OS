#include <stdio.h>

int main() {
    int b, p, i, j;
    scanf("%d", &b);
    int block[b];
    for(i=0;i<b;i++) scanf("%d", &block[i]);

    scanf("%d", &p);
    int process[p], alloc[p];
    for(i=0;i<p;i++){ scanf("%d", &process[i]); alloc[i] = -1; }

    for(i=0;i<p;i++){
        int wf=-1;
        for(j=0;j<b;j++)
            if(block[j]>=process[i]){
                if(wf==-1 || block[j]>block[wf]) wf=j;
            }
        if(wf!=-1){ alloc[i]=wf; block[wf]-=process[i]; }
    }

    printf("Process Size Block\n");
    for(i=0;i<p;i++)
        printf("%d\t%d\t%d\n", i+1, process[i], alloc[i]);
    return 0;
}

OUTPUT:
Process Size Block
1       212     4
2       417     4
3       112     1
4       426     4
